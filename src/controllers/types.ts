export enum UserErrorCodes {
  EMPTY = 'EMPTY',
  ALREADY_EXISTS = 'ALREADY_EXISTS',
}

export enum UserErrorMessages {
  EMPTY = 'Please, send your email and password',
  ALREADY_EXISTS = 'The user already exists',
}

export enum UserSuccessCodes {
  NEW_USER = 'NEW_USER',
}

export enum UserSuccessMessages {
  NEW_USER = 'New user saved successfully',
}
